server:
  port: 9000
  servlet:
    context-path:
spring:
  application:
    name: product
  datasource:
    url: jdbc:mysql://localhost:3306/event_queue
    username: root
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true